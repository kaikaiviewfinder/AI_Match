# 图寻 TU XUN — 前端技术文档

## 一、项目概述

**图寻（TU XUN）** 是一个多传感器融合徒步定位系统的 Web 前端可视化界面，服务于全国大学生电子设计大赛 Intel 杯嵌入式 AI 官方系列专题赛的演示场景。

前端通过 ROS WebSocket 协议与后端系统（VINS-Fusion + 粒子滤波路线匹配引擎）实时通信，将定位结果、传感器状态、轨迹数据以图形化方式呈现在浏览器中。

本项目包含两套前端实现：

| 版本 | 技术栈 | 入口 | 风格 |
|------|--------|------|------|
| V1（原型） | 原生 HTML + CSS + JS + Canvas 2D | `index.html` | 深色赛博朋克风 |
| V2（正式版） | React 19 + TypeScript + Tailwind CSS 4 + ECharts | `tuxun-ui/` | 明亮现代简约风 |

当前运行端口：**http://localhost:5174**（V2 版本）。

---

## 二、技术栈

| 类别 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 框架 | React | 19.2 | UI 组件框架 |
| 类型 | TypeScript | 6.0 | 类型安全 |
| 构建 | Vite | 8.1 | 开发服务器与构建 |
| 样式 | Tailwind CSS | 4.3 | 原子化 CSS |
| 图标 | Lucide React | 1.21 | SVG 图标库 |
| 动画 | Framer Motion | 12.42 | 声明式动画 |
| 图表 | ECharts + echarts-for-react | 6.1 | 数据可视化 |
| 地图 | Leaflet + react-leaflet | 1.9 / 5.0 | 地图组件（预留） |
| ROS 通信 | roslib.js | 2.1 | WebSocket 连接 ROS |
| 状态管理 | Zustand | 5.0 | 轻量状态管理 |
| 路由 | React Router DOM | 7.18 | 前端路由 |
| Lint | oxlint | 1.69 | 代码检查 |

---

## 三、系统架构

```
┌────────────────────────────────────────────────────────────┐
│                       浏览器 (localhost:5174)              │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                    App.tsx                            │ │
│  │              页面路由 + 状态分发                       │ │
│  └──────┬───────────────────────────────────────────────┘ │
│         │                                                  │
│         ▼                                                  │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              Layout.tsx                               │ │
│  │   侧边栏导航 + Header 状态栏 + Footer                  │ │
│  └──────┬───────────────────────────────────────────────┘ │
│         │                                                  │
│    ┌────┴─────────────────────────────┐                   │
│    ▼                                  ▼                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │Dashboard │  │Trajectory│  │ Sensors  │  │ Particles│ │ │
│  │ 仪表盘   │  │ 轨迹分析 │  │ 传感器   │  │粒子滤波  │ │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              useRosBridge Hook                         │ │
│  │      ROS WebSocket 连接管理 + 数据订阅 + 状态管理       │ │
│  └──────────────────────┬───────────────────────────────┘ │
└──────────────────────────┼────────────────────────────────┘
                           │ WebSocket ws://localhost:9090
                           │
┌──────────────────────────┼────────────────────────────────┐
│                   ROS Noetic 后端                          │
│                           │                                │
│  rosbridge_server         │                                │
│                           │                                │
│  ┌────────────────────────┼────────────────────────────┐  │
│  │  /route_matcher/state  │  /gps       │  /cam0/...   │  │
│  │  (JSON 定位状态)       │  (GPS坐标)  │  (MJPEG视频) │  │
│  └────────────────────────┴─────────────┴──────────────┘  │
│                                                            │
│  VINS-Fusion (VIO)  →  粒子滤波路线匹配  →  RViz 可视化   │
└────────────────────────────────────────────────────────────┘
```

### 数据流

1. 后端 C++/Python 节点处理传感器数据，通过 ROS topic 发布结果
2. `rosbridge_server` 将 ROS 消息桥接为 WebSocket 协议
3. 前端 `useRosBridge` Hook 订阅话题，解析 JSON，驱动 UI 更新
4. 实时相机画面通过 `web_video_server` 以 HTTP MJPEG 流单独传输

---

## 四、页面功能详解

### 4.1 全局布局 (Layout.tsx)

**侧边栏：** 可折叠导航菜单，宽度在 240px（展开）和 68px（收起）之间切换，带 CSS transition 动画。

导航项：

| 入口 | 图标 | 功能 |
|------|------|------|
| Dashboard | `LayoutDashboard` | 仪表盘总览，实时定位状态 |
| Trajectory Analysis | `Route` | 全屏轨迹地图 |
| Sensor Status | `Activity` | 传感器健康状态 |
| Particle Filter | `Cpu` | 粒子滤波运行状态 |
| System Config | `Settings` | ROS 节点与资源信息 |
| About | `Info` | 项目技术栈介绍 |

**侧边栏底部：** 实时显示 ROS 连接指示灯（绿色 Online / 红色 Offline），以及当前定位位置和置信度。

**顶部 Header：** 显示 GPS 延迟、当前海拔、VIO 步长、定位精度等关键指标。

**底部 Footer：** 系统名称 + 版本号 + 当前时间。

### 4.2 Dashboard 仪表盘页

主监控页面，数据最密集。

#### 4.2.1 四张 StatCard 指标卡片

| 卡片 | 显示内容 | 状态颜色逻辑 |
|------|----------|-------------|
| **Current Position** | 当前路线里程 + 路段 ID + 进度百分比 | 蓝色（信息） |
| **Localization Error** | ±标准差（米） | 绿 < 3m / 黄 3-8m / 红 > 8m |
| **GPS Status** | GPS 延迟时间 + 已收集点数 | 绿 < 5s / 黄 > 5s |
| **System Confidence** | 置信度百分比 + 位置样本数 | 绿 > 80% / 黄 50-80% / 红 < 50% |

每张卡片由 `StatCard` 组件渲染，包含图标（带彩色背景圆角方块）、大号数值、辅助说明文字。悬停时边框加深并出现微阴影。

#### 4.2.2 实时相机画面

- 嵌入 `http://localhost:8080/stream?topic=/cam0/image_raw&type=mjpeg&quality=60` 的 MJPEG 视频流
- 显示 VINS-Fusion 的特征追踪画面（KLT 光流的绿色特征点）
- 底部状态栏显示话题名、分辨率、连接指示灯
- 图片加载失败时自动隐藏

#### 4.2.3 轨迹地图 (Canvas 2D 自研渲染)

不使用第三方地图库，基于 Canvas 2D 自研实时渲染引擎，以 ENU（东-北-天）局部坐标系为基准。

**绘制层次（从底到顶共 6 层）：**

| 层序 | 内容 | 样式 | 说明 |
|------|------|------|------|
| 1 | 网格背景 | 浅灰线，40px 间距 | 视觉参考 |
| 2 | 已知参考路线 | 灰虚线 `#CBD5E1` | 从 route_table.csv 加载 |
| 3 | GPS 轨迹点 | 橙色圆点 `#F59E0B`，带发光 | 含 GPS 连接至最近路线点的连线 |
| 4 | VIO 全量路径 | 蓝虚线 `rgba(59,130,246,0.25)` | 展示 VIO 原始轨迹（包含漂移） |
| 5 | 最近 60 点匹配路径 | 蓝实线 `#2563EB` + 外发光 | 粒子滤波平滑后的路径 |
| 6 | 当前位置标识 | 蓝色实心圆 + 白边 + 脉冲光环 | 呼吸式脉冲动画，正弦波驱动 |
| 7 | 统计覆层 | 左上角文字 | GPS 点数、匹配位置、置信度 |

**坐标转换算法：**

```
1. 收集所有数据点（路线 + VIO + GPS），过滤离群值（|x|或|y| > 1e6）
2. 计算全局边界框（minX, maxX, minY, maxY）
3. 添加 15% 内边距防止元素贴边
4. 等比缩放 s = min((W-60)/rangeX, (H-50)/rangeY)
5. 屏幕坐标：cx = 30 + (px - minX + padX) × s
              cy = H - 25 - (py - minY + padY) × s
```

**GPS 经纬度 → ENU 局部坐标：** 使用 WGS84 椭球体墨卡托投影近似，以第一个路线点为原点。

#### 4.2.4 海拔信息面板

- 当前海拔大号数字（`matched_z`）
- 路线海拔范围（最低 ~ 最高）
- 海拔进度条：当前位置在海拔范围中的比例，蓝色填充

#### 4.2.5 位置详情面板

| 字段 | 数据源 | 说明 |
|------|--------|------|
| Latitude | `matched_lat` | 匹配后纬度，6 位小数 |
| Longitude | `matched_lon` | 匹配后经度，6 位小数 |
| Route Progress | `s_m` / `route_length_m` | 当前位置 / 路线总长 |
| Distance to End | `distance_to_end_m` | 距终点距离 |
| Heading | `yaw_rad` × 180/π | 航向角（度） |

#### 4.2.6 路线进度条

- 全宽渐变进度条（`#2563EB` → `#06B6D4`）
- 过渡动画 `duration-500`
- 底部标注 0m → 路线总长

### 4.3 Trajectory Analysis 轨迹分析页

专注轨迹可视化的全屏页面：

- **三张状态指示卡**：VIO Raw（蓝色）、Matched（蓝色 + 误差值）、GPS Track（橙色 + 点数）
- **全尺寸轨迹地图**：与 Dashboard 中相同的 Canvas 渲染组件，最小高度 500px
- **统计栏**：路线长度、路径采样数、GPS 点数、ROS 连接状态

### 4.4 Sensor Status 传感器状态页

**四张传感器卡片：**

| 传感器 | ROS Topic | 发布频率 | 状态 |
|--------|-----------|----------|------|
| Camera | `/cam0/image_raw` | 30 Hz | 绿色 Active |
| IMU | `/imu0` | 100 Hz | 绿色 Active |
| GPS | `/gps` | 1 Hz | 绿色 Active |
| BME280 | `/baro_altitude` | 20 Hz | 黄色 No Data（未连接时） |

每张卡片含：图标、状态指示灯（绿=正常/黄=无数据）、Topic 名称、频率、状态文字。

**实时传感器数据表：** 6 行 2 列的键值对网格，展示 VIO 步长、气压海拔、GPS 延迟、海拔误差、GPS 点数、ROS 状态。

### 4.5 Particle Filter 粒子滤波页

**滤波统计卡片：**

| 卡片 | 值 | 说明 |
|------|---|------|
| Particles | 1500 · Active | 粒子总数与状态 |
| Effective N | 动态计算值 | 有效粒子数，基于标准差估算 |
| Convergence | 标准差 (m) | 粒子收敛程度 |

**粒子分布动画 (Canvas 2D)：**

- 300 个粒子在画布上运动
- 每个粒子受中心引力（弹簧效应）+ 随机布朗扰动
- 速度每帧衰减 0.92（模拟阻尼）
- 粒子大小 = `1.8 × 权重 + 0.5`
- 不透明度 = `0.3 + 权重 × 0.5`
- 中心点固定为蓝色实心圆，表示当前估计位置
- 使用 `requestAnimationFrame` 驱动，60fps 流畅动画

**滤波更新步骤（5 步）：**

| 步骤 | 状态示例 | 颜色 |
|------|----------|------|
| Motion Update | VIO 步长 · noise ±0.5m | 蓝色 `#3B82F6` |
| GPS Update | 最近修正时间 · σ=12m | 橙色 `#F59E0B` |
| Altitude Update | 气压海拔 · σ=5m | 绿色 `#22C55E` |
| Slope Update | 路线坡度匹配 · σ=2m | 青色 `#06B6D4` |
| Resample | 触发/未触发（Neff 低时触发） | 灰色 `#64748B` |

每步前有彩色圆点指示，左侧标签（36 字符宽固定对齐），右侧详细说明。

### 4.6 System Config 系统配置页

**ROS 节点列表：** 7 个运行节点，每个显示节点名（等宽字体）、所属包名、绿色状态指示灯。

| 节点名 | 所属包 | 功能 |
|--------|--------|------|
| `/vins_node` | vins | VINS-Fusion 视觉惯性里程计 |
| `/route_matcher` | hiking_route_localization | 粒子滤波路线匹配器 |
| `/route_visualizer` | hiking_route_localization | RViz 路线可视化发布 |
| `/rosbridge_websocket` | rosbridge_server | WebSocket 通信桥接 |
| `/web_video_server` | web_video_server | HTTP MJPEG 视频流 |
| `/static_transform_publisher` | tf | map→world 静态坐标变换 |
| `/image_transport_republish` | image_transport | 图像传输中继 |

**系统资源信息：** ROS Master 连接状态、WebSocket 地址 (`ws://localhost:9090`)、前端技术栈、视频服务器地址、路线数据路径。

### 4.7 About 关于页

展示六项核心技术栈的卡片网格：

- VINS-Fusion — Visual-Inertial Odometry
- Particle Filter — Route Matching Engine
- ROS Noetic — Robot Operating System
- BME280 — Barometric Altitude
- GPS/BDS — Satellite Positioning
- DK-2500 — Embedded Platform

---

## 五、ROS 通信实现

### 5.1 WebSocket 连接

```
WebSocket URL: ws://localhost:9090
协议: rosbridge v2.0
```

`useRosBridge` Hook 在组件挂载时建立 WebSocket 连接，监听 `connection`、`close`、`error` 事件控制 `connected` 状态，组件卸载时调用 `ros.close()` 断开。

### 5.2 订阅话题

**`/route_matcher/state`** (`std_msgs/String`) — JSON 格式定位状态：

```typescript
{
  s_m: number;              // 路线里程 (m)，范围 0 ~ 537.9
  route_length_m: number;   // 路线总长 (m)
  progress: number;         // 进度 (0~1)
  segment_id: number;       // 路段编号
  distance_to_end_m: number;// 距终点距离 (m)
  matched_x: number;        // ENU X 坐标 (m)
  matched_y: number;        // ENU Y 坐标 (m)
  matched_z: number;        // 匹配海拔 (m)，范围约 -164 ~ 14
  matched_lon: number;      // 匹配经度，约 113.935°E
  matched_lat: number;      // 匹配纬度，约 22.53°N
  yaw_rad: number;          // 航向角 (弧度)
  s_std_m: number;          // 定位标准差 (m)
  confidence: number;       // 置信度 (0~1)
  last_vio_delta_m: number; // VIO 步长 (m)
  baro_altitude_m: number | null;  // 气压计海拔
  altitude_error_m: number | null; // 海拔误差
  gps_age_s: number | null; // GPS 延迟 (s)
}
```

**`/gps`** (`sensor_msgs/NavSatFix`) — GPS 定位，仅 `status.status >= 0` 时记录，缓存最近 120 个点。

### 5.3 视频流

```
URL: http://localhost:8080/stream?topic=/cam0/image_raw&type=mjpeg&quality=60
协议: HTTP MJPEG Stream
分辨率: 1280×720 (灰度)
压缩: JPEG quality 60
```

### 5.4 路线数据

`route_table.csv` 位于 `public/` 目录下，页面加载时通过 `fetch()` 读取解析。包含 270 个路线采样点（537.9m 总长），字段：`type, s, x, y, z, yaw, slope, curvature, segment_id, lon, lat`。

---

## 六、组件设计

### 6.1 组件树

```
App
 └── Layout
      ├── Sidebar (导航 + ROS 状态)
      ├── Header (GPS/海拔/VIO/精度 指标)
      ├── Main Content
      │    ├── Dashboard
      │    │    ├── StatCard × 4
      │    │    ├── Video Panel
      │    │    ├── TrajectoryMap (Canvas)
      │    │    ├── Altitude Panel
      │    │    ├── Position Panel
      │    │    └── Progress Bar
      │    ├── TrajectoryPage
      │    │    ├── Status Card × 3
      │    │    └── TrajectoryMap (Canvas)
      │    ├── SensorsPage
      │    │    ├── Sensor Card × 4
      │    │    └── Live Data Table
      │    ├── ParticlesPage
      │    │    ├── Stat Card × 3
      │    │    ├── Particle Canvas
      │    │    └── Filter Steps List
      │    ├── SystemPage
      │    │    ├── Nodes Table
      │    │    └── Resources Table
      │    └── AboutPage
      │         └── Tech Stack Grid
      └── Footer
```

### 6.2 StatCard 组件

通用统计指标卡片，接收参数：

| Prop | 类型 | 说明 |
|------|------|------|
| `label` | string | 指标名称 |
| `value` | string | 主值（大号加粗） |
| `sub` | string? | 辅助说明文字 |
| `icon` | LucideIcon | 图标组件 |
| `status` | 'ok'/'warn'/'error'/'info' | 颜色主题 |
| `className` | string? | 自定义样式 |

四种状态颜色映射：

| status | 背景 | 文字/图标 |
|--------|------|-----------|
| info | `#EFF6FF` | `#2563EB` (蓝) |
| ok | `#F0FDF4` | `#22C55E` (绿) |
| warn | `#FFFBEB` | `#F59E0B` (橙) |
| error | `#FEF2F2` | `#EF4444` (红) |

### 6.3 TrajectoryMap 组件

纯 Canvas 2D 轨迹地图，通过 props 接收数据，使用 `useEffect` + `useMemo` 实现高性能渲染：

- `useMemo` 预计算 GPS 局部坐标（依赖 GPS 数组、路线点、坐标转换函数）
- `useEffect` 中完成 Canvas 绑定、DPR 适配、坐标变换和 6 层绘制
- 数据变更时自动重绘（依赖 `routePoints`、`pathHistory`、`gpsLocal`）

---

## 七、工具函数 (lib/utils.ts)

```typescript
cn(...classes)           // 类名合并，过滤 falsy 值
formatCoord(val, 6)      // 经纬度格式化，固定 6 位小数
formatPercent(val)       // 小数 → 百分比字符串 "85.2%"
formatDist(m)            // 米 → "856 m" 或 "1.23 km"（≥1000m 时）
formatDuration(s)        // 秒 → "3:45" 格式
```

---

## 八、视觉设计系统

### 8.1 颜色体系 (Tailwind)

| 用途 | 颜色 | Tailwind Class |
|------|------|----------------|
| 主色调 | `#2563EB` | `blue-600` |
| 渐变辅色 | `#06B6D4` | `cyan-500` |
| 主文字 | `#0F172A` | `slate-900` |
| 次级文字 | `#64748B` | `slate-500` |
| 辅助文字 | `#94A3B8` | `slate-400` |
| 背景色 | `#F8FAFC` | `slate-50` |
| 卡片背景 | `#FFFFFF` | `white` |
| 边框 | `#E2E8F0` | `slate-200` |
| OK 状态 | `#22C55E` | `green-500` |
| 警告状态 | `#F59E0B` | `amber-500` |
| 错误状态 | `#EF4444` | `red-500` |

### 8.2 圆角系统

卡片和面板统一使用 `rounded-2xl`（16px 圆角）。图标背景使用 `rounded-xl`（12px 圆角）。

### 8.3 阴影与交互

- 卡片默认无阴影，hover 时出现 `hover:shadow-sm`
- 边框 hover 时从 `#E2E8F0` 过渡到 `#CBD5E1`
- 进度条使用 `transition-all duration-500` 平滑过渡

### 8.4 字体

- 系统字体栈：`Inter, system-ui, sans-serif`
- 等宽字体用于节点名、代码（Tailwind `font-mono`）
- 数值使用 `font-bold tracking-tight`（加粗 + 紧凑字距）

### 8.5 V1 原型风格 (index.html)

V1 采用深色赛博朋克风格：

- 主色调：青色 `#00e8d0`，暗背景 `#060612`
- 毛玻璃面板：`rgba(10,10,28,0.9)` + 半透明边框
- 当前点脉冲动画：`Math.sin(Date.now()/400)` 驱动圆半径和透明度正弦波动
- 网络摄像头直接连接：`web_video_server` MJPEG 流
- 底部指标栏：精度、置信度、位置、距终点、坐标、海拔、VIO 步长
- 海拔剖面 Canvas 子图：灰色虚线路高程 + 青色实线匹配海拔 + 当前位置脉冲点

---

## 九、启动与构建

### 开发环境

```bash
cd /home/kai/AI/fortend-demonstration-website/tuxun-ui
npm install
npm run dev
# → http://localhost:5173 (当前实际运行于 5174)
```

### 生产构建

```bash
npm run build        # 输出到 dist/
npm run preview      # 预览构建结果
```

### V1 原型

```bash
cd /home/kai/AI/fortend-demonstration-website
python3 -m http.server 8000
# → http://localhost:8000
```

### 完整系统

```bash
bash /home/kai/AI/fortend-demonstration-website/start_full.sh
```

启动 7 个进程：roscore → rosbridge → 前端 HTTP → VINS → TF → 路线匹配器 → RViz，然后播放 rosbag 数据。

---

## 十、项目文件结构

```
fortend-demonstration-website/
├── index.html                       # V1: 纯 HTML 赛博朋克风单页应用
├── route_table.csv                  # 参考路线数据 (270 点, 537.9m)
├── spec.txt                         # 比赛需求说明
├── start_full.sh                    # 一键完整系统启动脚本
├── 项目搭建与说明文档.md             # 项目搭建说明
│
└── tuxun-ui/                        # V2: React + TypeScript 正式版
    ├── package.json                 # 依赖与脚本
    ├── vite.config.ts               # Vite 配置 (port 5173, host 0.0.0.0)
    ├── tsconfig.json                # TypeScript 配置
    ├── index.html                   # Vite 入口
    ├── public/
    │   ├── favicon.svg
    │   └── icons.svg
    └── src/
        ├── main.tsx                 # ReactDOM.createRoot 入口
        ├── App.tsx                  # 根组件: 页面路由 + 状态分发
        ├── index.css                # Tailwind 全局样式
        ├── assets/                  # 静态资源
        ├── components/
        │   ├── Layout.tsx           # 全局布局 (侧边栏+Header+Footer)
        │   ├── StatCard.tsx         # 统计指标卡片
        │   └── TrajectoryMap.tsx    # Canvas 2D 轨迹地图
        ├── hooks/
        │   └── useRosBridge.ts      # ROS WebSocket 通信 + 类型定义
        ├── lib/
        │   └── utils.ts             # 工具函数 (格式化/类名合并)
        └── pages/
            ├── Dashboard.tsx        # 仪表盘: 指标+视频+地图+海拔+进度
            ├── Trajectory.tsx       # 轨迹全屏分析
            ├── Sensors.tsx          # 传感器健康状态
            ├── Particles.tsx        # 粒子滤波可视化
            └── System.tsx           # 系统配置与节点信息
```

---

## 十一、V1 与 V2 对比

| 维度 | V1 (index.html) | V2 (tuxun-ui) |
|------|-----------------|---------------|
| 设计风格 | 深色赛博朋克（暗底+青色高亮） | 明亮现代简约（白底+蓝色主题） |
| 技术栈 | 纯 HTML/CSS/JS，零构建 | React 19 + TypeScript + Vite |
| 页面 | 单页，上下分区 | SPA 多页，侧边栏导航 |
| 轨迹渲染 | Canvas 2D，含海拔子图 | Canvas 2D，组件化复用 |
| 海拔展示 | Canvas 绘制折线图 | 独立卡片 + 文字 + 进度条 |
| 文件规模 | 单文件 411 行 | 模块化 ~15 个文件 |
| 类型安全 | 无 | TypeScript 全量覆盖 |
| 粒子可视化 | 无 | Canvas 粒子物理动画 |
| 部署 | `python3 -m http.server` | `npm run dev` / `npm run build` |
| 扩展性 | 低，修改需编辑单文件 | 高，组件化 + Hook 抽象 |

---

## 十二、技术亮点

| 特性 | 实现方式 | 效果 |
|------|----------|------|
| 实时数据推送 | ROS WebSocket + `roslib.js` 订阅模式 | 后端状态更新即时反映到前端 |
| 自研轨迹渲染引擎 | Canvas 2D + 60fps `requestAnimationFrame` | 六层叠加渲染，流畅无卡顿 |
| 当前位置呼吸脉冲 | `Math.sin(Date.now()/600)` 驱动半径+透明度 | 醒目的周期性脉冲动画 |
| GPS 误差可视化 | GPS 到最近路线点的彩色连线 | 直观展示 GPS 偏移量与路线关系 |
| 粒子滤波动画 | Canvas 物理模拟（引力+阻尼+随机扰动） | 生动展示粒子收敛过程 |
| 坐标系统一 | 墨卡托投影 + 自动边界框检测 | GPS 与 ENU 坐标平滑映射 |
| 三级颜色编码 | 绿/橙/红 基于阈值的动态颜色 | 一眼判断系统状态 |
| 可折叠侧边栏 | CSS `transition-all duration-300` | 节省空间，适配不同屏幕 |
| 传感器状态卡 | 彩色状态灯 + 话题+频率信息 | 快速诊断传感器健康 |
| 路线进度条 | 渐变蓝色 `from-#2563EB to-#06B6D4` | 直观了解整体进度 |

---

*文档日期: 2026-06-28*
*项目路径: `/home/kai/AI/fortend-demonstration-website/`*

# 图寻 (Tu Xun) — 多传感器融合徒步定位系统 功能汇总表

## 系统架构

```
应用展示层   V1 原生 Web 前端  │  V2 React SPA   │  RViz 3D 可视化
Web 桥接层   rosbridge (WebSocket)  │  web_video_server (MJPEG)
核心算法层   路线匹配粒子滤波  │  VINS-Fusion VIO  │  ISA 气压海拔换算
传感器抽象层 NMEA GPS 驱动  │  BME280 气压驱动  │  STM32 串口协议适配
硬件层      手机 Camera+IMU  │  STM32F103+BME280  │  GPS 模组
工程支撑层   标定参数管理  │  rosbag 数据闭环  │  仿真数据生成器  │  一键部署
```

**设计理念：** 层间标准化 ROS 接口松耦合，传感器/算法/前端均可独立替换或并行迭代。

---

## 一、数据闭环与部署体系

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| rosbag 全量录制传感器原始数据（cam/imu/gnss/baro），一次采集可无限回放复现 | ✓ | rosbag 录制，bag 约 10.1GB/20min；`rosbag play --clock` 时钟驱动离线回放，支持 `--start/--duration` 分段播放指定路段 |
| 纯软件仿真模式——沿 CSV 路线合成 VIO+GPS+Baro 数据，零硬件依赖即可开发调试 | ✓ | `demo_sensor_player.py`：20Hz VIO + 1Hz GPS(45%丢星率可配) + 含噪 Baro，噪声/漂移参数全部通过 roslaunch 可调 |
| 五种运行模式覆盖全流程——仿真→回放→部分硬件→全硬件→混合 | ✓ | 4 个 launch 文件 + 1 个一键 shell 脚本：`demo_route_matcher.launch` / `hiking_simulation.launch` / `real_route_matcher.launch` / `hiking_full.launch` / `start_full.sh`，传感器均支持 `_simulate:=true` 条件切换和 `_enabled:=true/false` 条件启停 |
| 后端日志持久化与历史轨迹回放 | ✗ | 需开发 ROS bag 自动归档 + 状态日志存储 + 前端 timeline 回放控件，形成"采集→调试→验证→沉淀"的数据闭环 |
| 自动化测试与 CI/CD | ✗ | 需 rosbag 回归测试集 + GitHub Actions，使每次算法改动可量化验证效果 |

---

## 二、相机-IMU 标定与参数管理

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| 手机相机内参标定——pinhole 模型 + 径向/切向畸变系数 | ✓ | ORB-SLAM3 离线标定，1280×720，fx=996.49, fy=995.87, cx=651.79, cy=364.87, k1=0.0651, k2=-0.2370 |
| 手机 IMU 噪声参数标定——加速度计/陀螺仪噪声密度 + 随机游走 | ✓ | ORB-SLAM3 标定原始值需放大约 10 倍以适配消费级 MEMS（acc_n=0.1, gyr_n=0.01, acc_w=0.001, gyr_w=0.0001） |
| 相机-IMU 外参标定——6DOF 刚体变换 + 时间偏移在线估计 | ✓ | 固定外参（平移 ~3.1cm, 旋转近似 I），在线 `estimate_td=1`，初值 td=0.00181s（相机滞后 IMU，匀速运动时收敛慢） |
| 标定参数独立管理——标定源与运行时配置分离，可溯源可复制 | ✓ | `Mobile-GVIO-calib/` 独立目录存放 ORB-SLAM3+YAML 双格式标定记录，运行时从 `VINS-Fusion/config/hiking/` 读取副本 |

---

## 三、传感器驱动

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| NMEA GPS 串口读取——解析 GGA/RMC/GLL 输出标准 ROS NavSatFix | ✓ | `nmea_gps_node.py`：pyserial, 校验和验证, fix quality 过滤, 无新 GGA 时 5s 用 RMC 重发, 支持仿真正向（可配噪声/丢星率） |
| BME280+STM32 气压海拔采集——嵌入式传感终端 + 串口数据链路 | ✓ | STM32F103 软件 I2C 驱动 BME280（温湿压三通道），Bosch 双精度补偿，USART1 115200bps→PC 端 `bme280_altitude_node.py` 正则解析 + ISA 大气模型换算海拔，20Hz 输出，支持仿真 |
| STM32 多格式兼容海拔读取——灵活适配不同固件输出格式 | ✓ | `serial_altitude_node.py`：提取行末浮点数作海拔，兼容时间戳前缀/CSV/纯数值三种格式 |
| 串口通信异常恢复——断连自动重连与传感器在线故障检测 | ✗ | 当前 pyserial 无自动重连，无数据超时/校验异常动态告警 |

---

## 四、VINS-Fusion 视觉惯性里程计

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| 单目+IMU 紧耦合 VIO——滑窗非线性优化估计 6DOF 位姿+速度 | ✓ | HKUST VINS-Fusion，Ceres Solver，IMU 预积分，150 点 KLT 光流 @10Hz，求解器 40ms/8 迭代，发布 `/vins_estimator/odometry` |
| 在线时空标定——自动估计相机-IMU 时间偏移 | ✓ | `estimate_td=1`，td 初值 1.81ms，充分运动激励下可在线收敛 |
| 特征跟踪可视化输出——MJPEG 流供前端调试图传 | ✓ | `/vins_estimator/image_track` → `mjpeg_server.py`（OpenCV JPEG Q80）→ HTTP :8081 MJPEG 流 |
| 回环检测与全局位姿图优化 | ✗ | VINS-Fusion 支持 `loop_fusion`/`global_fusion` 但未集成；徒步线性路径回环少，GPS 融合在粒子滤波中松耦合完成 |

---

## 五、路线数据管理

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| GPX 路线导入——解析标准 GPX 格式自动生成 CSV 路线表 | ✓ | `gpx_to_route.py`：XML 解析 trkpt/rtept/wpt，1m 步长重采样，自动计算里程/航向/坡度/曲率 |
| 路线表高效查询——支持里程插值与 XY 邻近搜索 | ✓ | `RouteTable` 类：`interpolate(s)` 二分+线性插值 O(logN)，`nearest_by_xy()` 窗口搜索加速，WGS84↔ENU 互转 |
| 合成测试路线生成——参数化路径辅助算法验证 | ✓ | `generate_demo_route.py`：正弦弯道+起伏海拔，~1500m/1391 点，可自定义原点/长度/步长 |
| 路线表标准化中间格式——隔离数据来源与算法消费 | ✓ | CSV 11 字段 schema（s/x/y/z/yaw/slope/curvature/segment_id/lon/lat + origin 参考点），GPX/KML/自绘均可通过转换器接入 |
| 无高程 GPX 自动 DEM 补全 + 多 track 智能选择 + UTM 精确投影 | ✗ | 户外 App GPX 常有高程缺失；>50km 路线当前 ENU 近似误差累积 |

---

## 六、核心算法——粒子滤波路线约束定位

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| 1D 粒子滤波——状态沿路线里程 s∈[0,L] 分布，多传感器加权融合 | ✓ | 1200-1500 粒子，VIO 增量驱动预测，4 种观测量加权更新（GPS 位置/气压海拔/气压斜率趋势/运动连续性），SUS 低方差重采样（Neff<55% 触发） |
| 自动初始化与恢复——无 GPS 时全路线均匀分布，有 GPS 后高斯收敛 | ✓ | 首个 GPS fix 在路线 XY 最近点周围 σ=50m 初始化；定位偏差 >80m 自动重触发 GPS 锚定 |
| GPS 水平位置约束——WGS84→ENU 后距离似然，软门限防灭绝 | ✓ | 自适应 GPS σ，80m 软门限截断，>3s 过期丢弃，weight=0.8 |
| 气压计海拔约束——路线 Z vs 实测 Z 高斯似然 | ✓ | σ=5m，weight=1.0；GPS 锚定后自动计算气压偏置补偿天气波动 |
| 气压斜率趋势约束——短窗口 ΔZ 匹配增强上下坡区分 | ✓ | 8s baro 窗口 ΔZ vs 路线窗口 ΔZ，σ=2m，weight=0.6 |
| 运动连续性约束——防止粒子跳跃 | ✓ | 当前 s vs last_s+VIO 增量偏差，σ=1.5m，weight=0.9；VIO 步长 >5m 截断防跳变 |
| 状态输出与置信度——结构化 JSON + 路线约束里程计 + 进度 | ✓ | 17 字段 JSON（/route_matcher/state）+ matched_odometry + progress；confidence = max(0,1-σ_s/80) |
| 粒子多样性恢复 + 多模态分布检测（岔路/隧道场景） | ✗ | 粒子退化后仅靠 GPS 锚定恢复；仅输出单峰均值，无法表达候选位置的多模态不确定性 |

---

## 七、后端可视化（RViz）

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| RViz 路线与定位可视化——4 个 Marker 实时标注路线/位置/进度/文字 | ✓ | 灰色已知路线(下采样)+橙色已遍历段+红色当前位置球(5m)+白色文字(里程/段号/进度/置信度/终点距离)，2Hz 刷新 |
| VIO 原始 vs 路线约束轨迹同窗对比 | ✓ | 绿色 VIO Odometry + 蓝色 Matched Odometry，各保留 100 点历史，直观评估匹配效果 |
| MJPEG 视频流服务——ROS 图像话题转 HTTP 流供浏览器消费 | ✓ | `mjpeg_server.py`：订阅 `/vins_estimator/image_track`→OpenCV JPEG→HTTP multipart，多线程，:8081 |

---

## 八、前端可视化

### V1——原生 Web 版 (index.html)

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| 单文件零依赖前端——一个 HTML 即完整展示 | ✓ | 411 行，`python3 -m http.server 8000` 部署，CDN roslib.js，暗色赛博朋克风格 |
| ROS 实时数据 + MJPEG 视频 + Canvas 地图/海拔剖面 | ✓ | rosbridge WebSocket 订阅 state/gps，路线 7 层 Canvas 叠加渲染（网格/路线/GPS/VIO/匹配轨迹/位置脉冲动画/标注），底栏 7 指标 |

### V2——React SPA 版 (tuxun-ui)

| 计划目标 | 已实现 | 技术要点 |
|----------|--------|----------|
| 现代工程化 SPA——React 19 + TypeScript 6 + Vite 8 + Tailwind 4 | ✓ | 6 页面客户端路由，明亮现代风格，可折叠侧边栏(240/68px)，三色状态灯体系(绿/黄/红)，组件化 StatCard |
| ROS 数据 Hook——连接/订阅/解析/缓存一体化 | ✓ | `useRosBridge.ts`：3 路 topic 订阅，VIO 双策略对齐(偏移法+增量法)，GPS ENU 转换，路径 600 点/GPS 120 点缓存 |
| Dashboard 仪表盘——统计卡片+视频+轨迹地图+海拔+位置详情+渐变进度条 | ✓ | 4 StatCard 自适应颜色分级，Canvas 7 层地图(含 GPS 偏差连线+图例)，全宽渐变进度条(蓝→青) |
| 专项分析页面——轨迹分析/传感器监控/粒子滤波/系统配置/关于 | ✓ | 三路轨迹同窗对比、传感器状态卡片、300 粒子 Canvas 物理动画(60fps)、ROS 节点状态面板 |
| 断连自动重连 + 移动端响应式适配 + 未使用依赖清理 | ✗ | roslib.js 断连后状态丢失需手动刷新；仅桌面端布局；zustand/leaflet/echarts 在 dependencies 但未 import |

---

## 九、未实现 / 待开发功能

| 计划目标 | 状态 | 优先级 | 技术路线 |
|----------|------|--------|----------|
| 偏离路线实时告警 | ✗ | 高 | 需增加横向误差估计（当前仅 1D s 方向） |
| 后端数据持久化 + 历史轨迹回放 | ✗ | 高 | bag 自动归档 + 状态日志 DB + 前端 timeline 控件 |
| 自动化测试 CI/CD | ✗ | 中 | rosbag 回归测试集 + GitHub Actions + lint/typecheck |
| 多路线拓扑切换 | ✗ | 中 | 2D 粒子滤波 + 路线图拓扑模型 |
| 磁力计航向融合 | ✗ | 中 | 新增磁力计似然项，需软铁/硬铁校准 |
| 移动端适配 | ✗ | 中 | Tailwind 响应式断点 + 触摸手势 |
| 路线自动学习 + 多语言 i18n + GPS RTK | ✗ | 低 | — |

---

## 十、总体评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 系统化程度 | ★★★★☆ | 6 层架构清晰，数据闭环完整，5 种运行模式覆盖全流程 |
| 模块化程度 | ★★★★☆ | 层间标准化 ROS 接口，传感器/算法/前端独立替换、可组合启停 |
| 生态兼容性 | ★★★★☆ | 对接 GPX 户外生态 + ROS 机器人生态 + NMEA 定位生态 + Web 标准协议 |
| 工程成熟度 | ★★★☆☆ | 核心流水线可用，缺测试/CI/持久化/异常恢复 |
| 可扩展性 | ★★★★☆ | 观测源加权求和新传感器即插即融，前端双版本独立迭代 |

**核心亮点：** "一套代码、五种模式"全流程覆盖；层间标准化接口使传感器/算法/前端独立演进；从户外 GPX→ROS→Web 全链路标准协议对接；粒子滤波 4 种观测源加权融合框架可按需增减。

**关键提升方向：** 完善数据持久化与历史回放形成迭代闭环；增加偏离检测与多模态分布支持；建立自动化测试体系。
